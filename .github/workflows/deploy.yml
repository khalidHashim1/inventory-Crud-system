name: Deploy Frontend and Lambda

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js (needed for npx in S3 deploy)
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"  # latest LTS is fine

      # 3️⃣ Deploy frontend to S3 and invalidate CloudFront
      - name: Deploy frontend to S3 & invalidate CloudFront
        uses: reggionick/s3-deploy@v4
        with:
          folder: frontend
          bucket: ${{ secrets.S3_BUCKET_NAME }}
          bucket-region: ${{ secrets.AWS_REGION }}
          dist-id: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}
          invalidation: "/*"
          delete-removed: true
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      # 4️⃣ Package backend Lambda
      - name: Package Lambda
        run: |
          cd backend
          pip install -r requirements.txt -t .
          zip -r ../lambda.zip .

      # 5️⃣ Update Lambda function
      - name: Update Lambda function
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: ${{ secrets.AWS_REGION }}
          function_name: ${{ secrets.LAMBDA_FUNCTION_NAME }}
          zip_file: lambda.zip
